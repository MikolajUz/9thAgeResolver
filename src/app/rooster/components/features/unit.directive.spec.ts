import { UnitDirective } from './unit.directive';

describe('UnitDirective', () => {
  it('should create an instance', () => {
    const directive = new UnitDirective();
    expect(directive).toBeTruthy();
  });
});
